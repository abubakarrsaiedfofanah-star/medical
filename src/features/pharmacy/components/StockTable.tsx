import {AlertTriangle, MoreHorizontal} from 'lucide-react';
import type {Medicine} from '../types/pharmacy';

export default function StockTable({medicines}:{medicines:Medicine[]}){return <div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Category</th><th>Pack size</th><th>Stock level</th><th>Status</th><th/></tr></thead><tbody>{medicines.map((medicine)=><tr key={medicine.id}><td><strong>{medicine.name}</strong><small>{medicine.strength}</small></td><td>{medicine.category}</td><td>{medicine.packSize}</td><td><strong>{medicine.stock}</strong> units</td><td><span className={`stock-pill ${medicine.status.toLowerCase().replaceAll(' ','-')}`}>{medicine.status}</span></td><td><button type="button" className="table-more" title={`More options for ${medicine.name}`} onClick={()=>window.alert(`${medicine.name}: ${medicine.stock} units available`)}><MoreHorizontal size={17}/></button></td></tr>)}</tbody></table>{medicines.some((medicine)=>medicine.status !== 'In stock') && <div className="stock-note"><AlertTriangle size={16}/> Stock levels are based on your configured reorder thresholds.</div>}</div>}
