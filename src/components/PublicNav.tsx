import {useState} from 'react'; import {Link,useLocation} from 'react-router-dom'; import {ChevronDown,Menu,X} from 'lucide-react'; import BrandLogo from './BrandLogo';
const links=[['Home','/'],['Services','/services'],['Find Care','/providers'],['Pharmacy','/pharmacy'],['AI Health','/ai'],['Contact','/contact']];
export default function PublicNav(){const [open,setOpen]=useState(false); const location=useLocation(); return <nav className={`public-nav${open?' nav-open':''}`}><Link className="logo" to="/" onClick={()=>setOpen(false)}><BrandLogo/><span>SAIED<small>CONNECTED CARE</small></span></Link><div className="public-links" id="primary-navigation">{links.map(([label,to])=><Link to={to} key={to} aria-current={location.pathname===to?'page':undefined} onClick={()=>setOpen(false)}>{label}{label==='Services'&&<ChevronDown size={15}/>}</Link>)}</div><div className="nav-actions"><Link className="ghost-btn" to="/auth?mode=login">Sign in</Link><Link className="red-btn" to="/auth?mode=register">Enter SAIED <span className="nav-arrow">↗</span></Link></div><button type="button" className="mobile-menu" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} aria-controls="primary-navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav>}
